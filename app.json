{
  "environments": {
    "test": {
      "addons":[
        "heroku-redis",
        "heroku-postgresql"
      ],
      "scripts": {
        "test": "bundle exec rubocop && bundle exec brakeman && bundle exec bundle-audit check --update && bundle exec bundle leak check --update && bundle exec database_consistency",
        "review:": "bundle exec rubocop && bundle exec brakeman && bundle exec bundle-audit check --update && bundle exec bundle leak check --update && bundle exec database_consistency"
      }
    }
  }
}
