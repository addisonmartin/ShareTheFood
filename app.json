{
  "environments": {
    "test": {
      "addons":[
        "heroku-redis",
        "heroku-postgresql"
      ],
      "scripts": {
        "test-setup": "bundle install && yarn install && bundle exec rails assets:precompile",
        "test": "bundle exec rubocop && bundle exec brakeman && bundle exec bundle-audit check --update && bundle exec bundle leak check --update && bundle exec database_consistency && bundle exec rspec",
        "review:": "bundle exec rubocop && bundle exec brakeman && bundle exec bundle-audit check --update && bundle exec bundle leak check --update && bundle exec database_consistency && bundle exec rspec"
      }
    }
  }
}
