<div class="row justify-content-center">
  <%= form_tag donations_path, method: :get, class: 'form-inline' do %>
    <%= text_field_tag 'search', params[:search], placeholder: "Search donation's name, description, pickup notes, ..." %>
    <%= submit_tag 'Search', class: 'btn btn-primary' %>
  <% end %>
</div>