<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="<%= root_path %>">Share the Food</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse order-1" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="<%= about_path %>">About</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="<%= donations_path %>">View Donations</a>
      </li>
    </ul>

    <div class="collapse navbar-collapse order-2">
      <%== user_links_for_navigation_bar %>
    </div>
  </div>
</nav>