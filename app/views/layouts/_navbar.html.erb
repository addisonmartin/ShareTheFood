<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="<%= root_path %>">Share the Food</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation bar">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="<%= about_path %>">About<span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="<%= donations_path %>">View Donations</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="<%= new_donation_path %>">Add a Donation</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="<%= posts_path %>">View Posts</a>
      </li>
    </ul>
    <%== user_authentication_links %>
  </div>
</nav>